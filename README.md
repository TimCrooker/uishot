# uishot

Instant, addressable UI screenshots for AI agents. One command returns fresh screenshots of any screen, any named state, any viewport sizes, in ~1–2s warm. Full README lands with v1 (see `docs/superpowers/specs/`).
